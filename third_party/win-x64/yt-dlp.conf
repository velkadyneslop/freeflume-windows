# FreeFlume — yt-dlp defaults applied to every invocation (playback via mpv's ytdl hook,
# plus our search/details/download calls). The android player client exposes a playable
# (non-DRM) format for some videos the default web client reports as "unavailable".
--extractor-args "youtube:player_client=default,android"
