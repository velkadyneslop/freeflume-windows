<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="FreeFlume.Views.PlaylistsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:FreeFlume.Models"
    mc:Ignorable="d">
    <Grid Padding="16,16,8,8" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: playlists -->
        <Grid Grid.Column="0" RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Playlists" Style="{ThemeResource TitleTextBlockStyle}" />
            <ListView x:Name="PlaylistsView" Grid.Row="1" SelectionMode="Single"
                      SelectionChanged="OnPlaylistSelected">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Playlist">
                        <TextBlock Text="{x:Bind Display}" Padding="0,8" TextTrimming="CharacterEllipsis" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
                <Button x:Name="NewButton" Click="OnNewClick">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                        <TextBlock Text="New" />
                    </StackPanel>
                </Button>
                <Button x:Name="DeleteButton" Click="OnDeleteClick" IsEnabled="False">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE74D;" FontSize="14" />
                        <TextBlock Text="Delete" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Right: items in selected playlist -->
        <Grid Grid.Column="1">
            <ListView x:Name="ItemsView" IsItemClickEnabled="True" SelectionMode="None"
                      CanReorderItems="True" AllowDrop="True" ItemClick="OnItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:SearchResult">
                        <Grid ColumnSpacing="12" Padding="0,6" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="120" Height="68" CornerRadius="6" Background="Black">
                                <Grid>
                                    <Image Source="{x:Bind Thumbnail}" Stretch="UniformToFill" />
                                    <Grid VerticalAlignment="Bottom" Height="3" Background="#66000000" Visibility="{x:Bind ProgressVisibility}">
                                        <Rectangle HorizontalAlignment="Left" Width="{x:Bind ProgressWidth}" Fill="#FFFF0000" />
                                    </Grid>
                                </Grid>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="{x:Bind Title}" MaxLines="2" TextWrapping="Wrap"
                                           TextTrimming="CharacterEllipsis"
                                           Style="{ThemeResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="{x:Bind Meta, Mode=OneWay}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Style="{ThemeResource CaptionTextBlockStyle}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock x:Name="EmptyText" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Text="Create a playlist, then add videos from Search." />
        </Grid>
    </Grid>
</Page>
